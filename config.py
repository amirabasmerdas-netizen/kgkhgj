"""
AMEL SELF55 - فایل تنظیمات اصلی
پیکربندی کلی پروژه
"""

import os
from dotenv import load_dotenv

load_dotenv()

# ─── تنظیمات Telegram API ───────────────────────────────────────────────────
API_ID = int(os.getenv("API_ID", "0"))
API_HASH = os.getenv("API_HASH", "")

# ─── تنظیمات Flask ──────────────────────────────────────────────────────────
SECRET_KEY = os.getenv("SECRET_KEY", "amel-self55-secret-key-2025")
PORT = int(os.getenv("PORT", "5000"))
HOST = os.getenv("HOST", "0.0.0.0")

# ─── مسیر دیتابیس ───────────────────────────────────────────────────────────
DATABASE_PATH = os.getenv("DATABASE_PATH", "amel_self55.db")

# ─── منطقه زمانی ─────────────────────────────────────────────────────────────
TIMEZONE = "Asia/Tehran"

# ─── تنظیمات ضد اسپم ────────────────────────────────────────────────────────
SPAM_DELAY = float(os.getenv("SPAM_DELAY", "2.0"))       # تأخیر بین پیام‌های اسپم (ثانیه)
SPAM_MAX = int(os.getenv("SPAM_MAX", "50"))               # حداکثر تعداد پیام اسپم

# ─── تعداد اسلات‌های ذخیره پیام ─────────────────────────────────────────────
MESSAGE_SLOTS = 10

# ─── تنظیمات ترجمه ───────────────────────────────────────────────────────────
TRANSLATION_API = os.getenv("TRANSLATION_API", "")       # اختیاری

# ─── نسخه ────────────────────────────────────────────────────────────────────
VERSION = "1.0.0"
BOT_NAME = "AMEL SELF55"

# ─── محدودیت کاربران: نامحدود ────────────────────────────────────────────────
# هیچ محدودیتی برای تعداد کاربران ثبت‌نام‌شده وجود ندارد
MAX_USERS = None  # None = نامحدود
